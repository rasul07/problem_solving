package main

import "fmt"

// NOT DONE

func sortingBooks(n, k int32, books string) string {
	var res string

	return res
}

func main() {
	var (
		n, k  int32
		books string
	)
	n = 12
	k = 3
	books = "cabccadabaac"
	// fmt.Scanf("%d", &n)
	// fmt.Scanf("%d", &k)
	// fmt.Scanf("%s", &books)

	fmt.Println(sortingBooks(n, k, books))
}
